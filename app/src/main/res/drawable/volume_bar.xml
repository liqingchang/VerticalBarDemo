<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 背景 -->
    <item android:id="@android:id/background" android:drawable="@drawable/bg_volume_progress_n">
    </item>

    <!-- 进度条式样 -->
    <item android:id="@android:id/progress">
        <clip>
            <layer-list>
                <item android:drawable="@drawable/bg_volume_progress_h"/>
            </layer-list>
        </clip>
    </item>

</layer-list>

